#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
PS1='[\u@\h \W]\$ '
export PF_INFO="ascii title os host kernel pkgs editor wm memory palette"
pfetch
